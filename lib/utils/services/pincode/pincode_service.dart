// import 'dart:async';

class PinCodeService {
  // Timer? _timer;

  // Duration get _duration => const Duration(seconds: 3);

  void init() {
    // _timer = Timer.periodic(_duration, (timer) {});
  }
}
